import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    header:{
        color: '#AAA',
        fontSize: 30,
        paddingLeft: 15,
    },
    container:{
        marginBottom: 15
    }
});

export default styles;